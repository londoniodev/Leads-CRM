# String de conexión a la base de datos PostgreSQL en Dokploy o externa
DATABASE_URL="postgresql://postgres:tu_password@72.62.161.199:5435/leads_generator_db?schema=public&connection_limit=10&pool_timeout=10"

# Entorno de Node.js
NODE_ENV="production"

# Puerto en el que escucha el contenedor
PORT=3000

# Hostname obligatorio en Docker para binding global
HOSTNAME="0.0.0.0"

# Token de la API de Apify para la ejecución de Scrapers B2B
APIFY_API_TOKEN="tu_apify_api_token_aqui"

# URL del webhook del motor backend (Fastify + BullMQ + Redis) para recibir la notificación de Apify
PROCESSOR_WEBHOOK_URL="http://72.62.161.199:3001/webhooks/apify/leads"

# Token secreto de autenticación del webhook configurado en Leads_Generator (Header x-webhook-secret)
WEBHOOK_SECRET_TOKEN="xX6+0+EuTlUynI/USQli6I14OgrVg3dAqnrzTkuOV8w="

# Clave API de Google Gemini para el Motor de IA y Copiloto de Propuestas
GEMINI_API_KEY="tu_gemini_api_key_aqui"
GEMINI_MODEL="gemini-2.0-flash"

